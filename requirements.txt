# Full local stack — everything needed to run the pipeline with Ollama.
# Equivalent to: pip install -e ".[all,dev]"
#
# Ollama must also be installed separately (https://ollama.com) and models pulled:
#   ollama pull nomic-embed-text
#   ollama pull llama3.2:3b

# Core
pydantic>=2.0

# Ingestion
markitdown[pdf]>=0.1.0

# Retrieval
chromadb>=0.5
rank-bm25>=0.2

# Reranking (optional — remove if you don't need cross-encoder reranking)
sentence-transformers>=3.0

# Local LLM provider
ollama>=0.4.0

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.14
